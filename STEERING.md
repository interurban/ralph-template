# Steering

Critical work that must be completed before regular tasks. The agent checks this file at the start of each iteration.

If items exist below, complete them **in order** before proceeding to `tasks/CURRENT.md`. Remove each item after completion.

---

<!-- Add critical/priority items here while the agent is running. Example:

## Fix broken authentication
The login flow is returning 500 errors. Fix this before continuing with feature work.
Debug the issue in src/app/api/auth/route.ts.

-->
